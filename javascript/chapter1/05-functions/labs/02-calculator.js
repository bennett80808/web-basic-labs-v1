// TODO: add, subtract, multiply, divide 함수 만들고 결과 출력

function add(a, b) {
  console.log(`a + b = ${a + b}`);
}

function subtract(a, b) {
  console.log(`a - b = ${a - b}`);
}
function multiply(a, b) {
  console.log(`a * b = ${a * b}`);
}
function divide(a, b) {
  console.log(`a / b = ${a / b}`);
}

add(1, 3);
subtract(1, 3);
multiply(1, 3);
divide(1, 3);
